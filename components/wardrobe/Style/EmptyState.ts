import { Platform, StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    emptyState: {
      alignItems: 'center',
      padding: 24,
    },
    countText: {
      fontSize: 16,
      marginBottom: 4,
    },
    helperText: {
      fontSize: 14,
      color: '#666',
      textAlign: 'center',
    },
  });